@model IEnumerable<Student>
@{ 
    ViewBag.Title = "学生列表";
        }
<div class=" card-deck" style="width:1000px;margin:auto">
    @foreach (var student in Model)
    {

        //var photoFile = "/images/" + (student.PhotoPath ?? "3.gif");
<div class="card" style="float:left;width:32%;margin:5px">
    <div class="card-header">
        <h3>@student.Name</h3>
    </div>
    <img class="card-img-top " height="200" src="/images//img1.jpg" asp-append-version="true"/>
    <div class="card-footer text-center">
        @*<a  href="/home/Details/@student.Id" class="btn btn-primary" >查看</a>*@
        @*使用TagHelper*@
        <a class="btn btn-primary" asp-controller="home" asp-action="details" asp-route-id="@student.Id">查看</a>
        <a href="/home/details/@student.Id"  class="btn btn-primary">查看</a>
        <a href="#" class="btn btn-primary">编辑</a>
        <a href="#" class="btn btn-danger">删除</a>
    </div>
</div>}
</div>